server {

    listen 80;

    server_name grafana.jrwroberts.co.uk;

    location / {

        proxy_pass http://grafana:3000;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

    }
}
